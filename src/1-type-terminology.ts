const typeInference = 'Value';
const typeAnnotation: string = 'Value';
const typeAssertion = 'Value' as string;

// Inspect variables with "IntelliSense"

// Fake a type with `as`

// Show type widening
// let wideText = 'Hello TypeScript Berlin Meetup!';

// Narrow down a wide text with if & if
